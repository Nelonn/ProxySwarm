syntax = "proto3";
package proxyswarm;
option go_package = "./pb";

import "node/common.proto";

message NaiveProxyConfig {
    string network = 1; // "tcp", "udp", or empty for both
    string quic_congestion_control = 2;
    TLSConfig tls = 3;
}
